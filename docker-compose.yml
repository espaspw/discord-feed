services:
  feed-manager:
    build: .
    container_name: danbooru-feed-manager
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./db:/app/db
    environment:
      - NODE_ENV=production